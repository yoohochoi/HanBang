<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hanbang.store.mapper.HouseMapper">

	<resultMap type="House" id="HouseMap">
	<collection property="publicUsages" column = "houseId"
	select = "hanbang.store.mapper.PublicUsageMapper.retriveAll"
	javaType="ArrayList" jdbcType="NUMERIC"></collection>
	</resultMap>

	<insert id="create" parameterType="House">
		<selectKey keyProperty="houseId" resultType="int" order="BEFORE">
			SELECT houseId_seq.nextval FROM dual
		</selectKey>
		INSERT INTO HOUSE_TB(
		houseId , ADDRESS, WHOLEAREA, BATHROOMS, ROOMS, MEMBERID)
		VALUES (#{houseId}, #{address}, #{wholeArea}, #{bathrooms}, #{rooms}, #{memberId})
	</insert>

	<select id="retriveByMemberId" parameterType="String"
		resultMap="HouseMap">
		SELECT ADDRESS, WHOLEAREA, PUBLICUSAGE, BATHROOMS, ROOMS
		FROM HOUSE_TB
	</select>

	<select id="retrive" parameterType="int" resultMap="HouseMap">
		SELECT
		ADDRESS, WHOLEAREA, PUBLICUSAGE, BATHROOMS, ROOMS
		FROM HOUSE_TB
		WHERE HOUSEID = (SELECT HOUSEID
		FROM sharehouse_tb
		WHERE
		house_tb.houseid = #{houseId}
		)
	</select>

	<update id="update" parameterType="House">
		UPDATE
		HOUSE_TB
		SET ADDRESS, WHOLEAREA, PUBLICUSAGE, BATHROOMS, ROOMS
		WHERE
		HOUSEID = #{houseId}
	</update>

	<delete id="delete" parameterType="int">
		DELETE FROM
		HOUSE_TB
		WHERE HOUSEID = #{houseId}
	</delete>

	<delete id="deleteByMemberId" parameterType="String">
		DELETE FROM
		HOUSE_TB
		WHERE MEMBERID =(SELECT MEMBERID
		FROM MEMBER_TB
		WHERE HOUSE_TB.MEMBERID = #{memberId})
	</delete>


</mapper>